apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: antik-moderne
spec:
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      nodePort: 30032
  type: NodePort